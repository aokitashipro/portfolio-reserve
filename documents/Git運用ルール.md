# Git運用ルール

## 🌐 言語設定（重要）

### ✅ 日本語で書くもの
- **コミットメッセージ**: すべて日本語
- **PR（Pull Request）のタイトル**: 日本語
- **PR説明文**: 日本語
- **Issue**: 日本語
- **コードコメント**: 日本語
- **ドキュメント**: 日本語

### ✅ 英語で書くもの
- **ブランチ名**: 英語（例: `feature/user-authentication`）
- **変数名・関数名**: 英語（例: `getUserById`）
- **ファイル名**: 英語（例: `BookingForm.tsx`）

---

## 📝 コミットメッセージフォーマット

### 基本形式

```
<プレフィックス>: <変更内容の要約>

<詳細説明（オプション）>

<関連Issue（オプション）>
```

### プレフィックス一覧

| プレフィックス | 用途 | 例 |
|--------------|------|-----|
| `feat` | 新機能追加 | `feat: ユーザー登録機能を実装` |
| `fix` | バグ修正 | `fix: 予約カレンダーの日付選択バグを修正` |
| `test` | テスト追加・修正 | `test: 予約APIのE2Eテストを追加` |
| `refactor` | リファクタリング | `refactor: BookingFormコンポーネントを分割` |
| `style` | コードスタイル修正 | `style: ESLint警告を修正` |
| `docs` | ドキュメント変更 | `docs: READMEにセットアップ手順を追記` |
| `chore` | ビルド・設定変更 | `chore: 依存パッケージを更新` |
| `perf` | パフォーマンス改善 | `perf: 画像の遅延読み込みを実装` |

### 良い例

```bash
# ✅ GOOD
feat: ユーザー登録機能を実装

- Supabase Auth統合
- Zodバリデーション追加
- 確認メール送信機能
- E2Eテスト作成

Closes #5

# ✅ GOOD
fix: 予約カレンダーで過去の日付が選択できてしまう問題を修正

過去の日付はdisabledプロパティを追加して選択不可にした。

Closes #12

# ✅ GOOD
test: 管理者ダッシュボードのE2Eテストを追加

# ✅ GOOD
docs: API仕様書を追加
```

### 悪い例

```bash
# ❌ BAD: 英語
feat: Add user registration feature

# ❌ BAD: 曖昧
Update files

# ❌ BAD: プレフィックスなし
ユーザー登録機能

# ❌ BAD: 詳細すぎる
feat: src/app/register/page.tsxにユーザー登録フォームを追加し、src/lib/supabase/auth.tsにSupabase Auth統合機能を追加し...
```

---

## 🔀 ブランチ命名規則

### フォーマット

```
<種類>/<機能名>
```

### 種類

- `feature/` - 新機能
- `fix/` - バグ修正
- `refactor/` - リファクタリング
- `test/` - テスト追加
- `docs/` - ドキュメント

### 良い例

```bash
# ✅ GOOD
feature/user-authentication
feature/booking-calendar
feature/admin-dashboard
fix/reservation-validation
refactor/component-structure
test/e2e-booking-flow
docs/api-specification
```

### 悪い例

```bash
# ❌ BAD: 日本語
feature/ユーザー認証

# ❌ BAD: スペース
feature/user authentication

# ❌ BAD: 大文字
feature/UserAuthentication

# ❌ BAD: プレフィックスなし
user-authentication
```

---

## 📋 PR（Pull Request）フォーマット

### タイトル

```
[プレフィックス] 変更内容の要約
```

### 例

```
[FEATURE] ユーザー登録機能を実装
[FIX] 予約カレンダーのバグを修正
[TEST] E2Eテストを追加
[DOCS] API仕様書を作成
```

### PR説明文テンプレート

```markdown
## 📝 概要
<!-- 何を実装したか、何を修正したかを簡潔に -->

ユーザーが新規登録できる機能を実装しました。

## 🎯 関連Issue
<!-- IssueがあればCloses #XX で自動クローズ -->

Closes #5

## ✅ 実装内容
<!-- チェックリスト形式で -->

- [x] `/register` ページ作成
- [x] Supabase Auth統合
- [x] Zodバリデーション実装
- [x] E2Eテスト作成
- [x] 確認メール送信機能

## 🧪 テスト方法
<!-- レビュアーが確認できるように具体的な手順 -->

1. `npm run dev` で開発サーバーを起動
2. http://localhost:3000/register にアクセス
3. フォームに以下を入力:
   - メール: test@example.com
   - パスワード: password123
4. "登録" ボタンをクリック
5. 確認メールが送信されることを確認
6. ログインページにリダイレクトされることを確認

## 🖼️ スクリーンショット
<!-- UI変更があれば画像を添付 -->

![登録画面](./screenshots/register.png)

## 📊 品質チェック
<!-- すべてチェックしてからPR作成 -->

- [x] ESLintエラー0件
- [x] TypeScriptエラー0件
- [x] ビルド成功
- [x] 単体テスト通過
- [x] E2Eテスト通過
- [x] テストカバレッジ 85%
- [x] レスポンシブ対応
- [x] アクセシビリティチェック

## 💡 レビューポイント
<!-- 特に見てほしい箇所 -->

- バリデーションロジックが適切か
- エラーハンドリングが十分か
- セキュリティ上の問題がないか

## 📝 補足
<!-- その他の情報 -->

Supabase Authの設定でメール確認を有効化する必要があります。
```

---

## 🔄 ワークフロー

### 1. ブランチ作成

```bash
# mainから最新を取得
git checkout main
git pull origin main

# 新しいブランチ作成
git checkout -b feature/user-authentication
```

### 2. 開発・コミット

```bash
# 変更を確認
git status
git diff

# ステージング
git add src/app/register/

# コミット（日本語で）
git commit -m "feat: ユーザー登録フォームを実装"
```

### 3. プッシュ

```bash
git push origin feature/user-authentication
```

### 4. PR作成

GitHubでPRを作成（タイトル・説明文は日本語）

### 5. マージ後

```bash
# ブランチ削除
git checkout main
git pull origin main
git branch -d feature/user-authentication
```

---

## 🚨 worktree運用時の注意

### 毎朝のrebase

```bash
# メインリポジトリ
cd /Users/a-aoki/indivisual/2026/portpfolio/reserve-system
git checkout main
git pull origin main

# 各worktreeでrebase
cd /Users/a-aoki/indivisual/2026/portpfolio/reserve-system-cicd
git rebase main

cd /Users/a-aoki/indivisual/2026/portpfolio/reserve-system-auth
git rebase main

cd /Users/a-aoki/indivisual/2026/portpfolio/reserve-system-booking
git rebase main

cd /Users/a-aoki/indivisual/2026/portpfolio/reserve-system-admin
git rebase main
```

### コンフリクト解決

```bash
# コンフリクト発生時
git status  # コンフリクトファイル確認

# エディタで手動解決（<<<<< HEAD などを削除）

# 解決後
git add .
git rebase --continue

# 解決できない場合は中止
git rebase --abort
```

---

## 📏 コミットの粒度

### 適切な粒度

```bash
# ✅ GOOD: 1つの機能ごと
git commit -m "feat: ユーザー登録フォームを実装"
git commit -m "test: ユーザー登録のE2Eテストを追加"
git commit -m "docs: ユーザー登録のドキュメントを追加"

# ❌ BAD: すべてまとめて
git commit -m "feat: ユーザー登録機能全部"

# ❌ BAD: 細かすぎ
git commit -m "feat: メールフィールド追加"
git commit -m "feat: パスワードフィールド追加"
git commit -m "feat: ボタン追加"
```

---

## 🎯 マージ戦略

### マージ順序（推奨）

```
1. feature/ci-cd-infrastructure → main
2. feature/user-authentication → main
3. feature/booking-system → main
4. feature/admin-dashboard → main
```

### マージ方法

- **Squash and merge**: 1つのIssueに対して複数コミットがある場合
- **Create a merge commit**: 大きな機能で履歴を残したい場合
- **Rebase and merge**: 線形履歴を保ちたい場合

---

## 📅 デイリーチェックリスト

### 朝（作業開始前）

- [ ] mainブランチを最新化
- [ ] 各worktreeでrebase
- [ ] コンフリクトがないか確認

### 夜（作業終了前）

- [ ] ESLint実行
- [ ] テスト実行
- [ ] コミット（日本語で）
- [ ] プッシュ

---

## 🔍 レビュー基準

### レビュアーが確認すること

- [ ] コミットメッセージが日本語か
- [ ] PRタイトル・説明が日本語か
- [ ] テストが追加されているか
- [ ] ドキュメントが更新されているか
- [ ] コードスタイルが統一されているか
- [ ] セキュリティ上の問題がないか

---

## 📚 参考

### コミットメッセージの例文

```bash
feat: 予約カレンダーコンポーネントを実装
feat: 空き時間取得APIを追加
fix: 予約日が過去の場合のバリデーションエラーを修正
test: 予約作成のE2Eテストを追加
refactor: 予約フォームを複数のコンポーネントに分割
style: 予約一覧のレイアウトを調整
docs: API仕様書に予約エンドポイントを追記
chore: Prismaクライアントを再生成
perf: 予約一覧のクエリを最適化
```

---

**最終更新**: 2025-12-31
